# Keep the build daemon within typical CI and developer-machine limits while
# native emulator cores are compiling in parallel.
org.gradle.jvmargs=-Xmx1536m -Dfile.encoding=UTF-8
org.gradle.workers.max=2
android.useAndroidX=true
